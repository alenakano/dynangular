import { ComponentRefDirective } from './component-ref.directive';

describe('ComponentRefDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentRefDirective();
    expect(directive).toBeTruthy();
  });
});
